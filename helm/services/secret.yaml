# Replace placeholder values with real Twilio credentials before applying.
# Do NOT commit real credentials — use `kubectl create secret` or a secrets manager instead.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  TWILIO_ACCOUNT_SID: "your_account_sid"
  TWILIO_AUTH_TOKEN: "your_auth_token"
  TWILIO_WHATSAPP_FROM: "whatsapp:+14155238886"
